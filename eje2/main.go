package main

import "fmt"

func main() {
	var estado bool
	estado = true
	if estado = false; estado == true { //Sd pueden hacer asigaciones dentro del condicional
		fmt.Println(estado)
	} else {
		fmt.Println(estado)
	}
}
